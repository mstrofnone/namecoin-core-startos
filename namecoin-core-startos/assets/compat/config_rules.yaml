# Configuration rules for Namecoin Core
# These rules enforce logical constraints between config options

# If pruning is enabled, txindex must be disabled
- rule: |
    if:
      pruning.mode:
        equals: "automatic"
    then:
      txindex:
        equals: false
        set: false
        description: "Transaction index cannot be used with pruning enabled"
